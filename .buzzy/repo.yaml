native:
  # A list of Buzzy package names mapped to the corresponding RHEL package.
  rpm:
    pkg-config: pkgconfig

preinstalled:
  # A list of packages that are preinstalled on Mac OS X.
  homebrew:
    libpcap: 1.1.1
    zlib: 1.2.5
